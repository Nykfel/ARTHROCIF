
data <- read.delim("C:/Users/2026mr001/Desktop/Stage/Temporaire/data.txt", header=FALSE)
max(data$V17, data$V18) - min(data$V34, data$V35)
max(data$V34, data$V35) - min(data$V17, data$V18)

library(dplyr)

data <- data %>%
  mutate(
    ordre = if_else(V17 < V34, "CifA", "CifB"),
    nouvelle_colonne = if_else(
      ordre == "CifA",
      pmin(V34, V35) - pmax(V17, V18),
      pmin(V17, V18) - pmax(V34, V35)
    )
  )


plot(data$nouvelle_colonne)
